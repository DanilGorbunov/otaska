{
  "version": 2,
  "buildCommand": "npm install --prefix frontend && npm --prefix frontend exec vite build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index.py" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
