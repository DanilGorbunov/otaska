// Platform commission: charged to the performer only, deducted from payout on
// booking completion. Clients pay exactly the price they see — no added fees.
// Wire this into the Stripe Connect transfer once escrow (Block 1) lands.
export const PLATFORM_COMMISSION_RATE = 0.12
